import 'package:flutter/material.dart';

class DetailPageTablet extends StatelessWidget {
  const DetailPageTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
